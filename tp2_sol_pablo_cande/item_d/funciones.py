def derivada_x(t, x, y):

    # a = 1.2
    # b = 0.6
    return (2.4 * x) - (1.2 * x * y)


def derivada_y(t, x, y):

    # c = 0.3
    # d = 0.8
    # hay un error de enunciado, habia que intercambiarlos
    return (0.6 * x * y) - (1.6 * y)